# 변경 이력

## 1.6.2 - 2026-09-02

- 두 위치 엔티티를 사람형 위치 핀, 거리 연결선, 근접 신호로 표현한 새 기본 아이콘으로 교체했습니다.
- 파랑·청록·보라 그라데이션과 굵은 실루엣을 사용해 Home Assistant의 32px 축소 화면에서도 의미가 구분되도록 개선했습니다.
- 라이트·다크 테마용 256px 및 512px 투명 PNG를 통합 패키지 내부 로컬 브랜드 자산으로 포함했습니다.
- 센서 계산, 엔티티 ID, 자동화 이벤트 및 설정 동작은 변경하지 않았습니다.

## 1.6.1 - 2026-09-01

- 실제로 변경된 위치 엔티티만 해당 측의 갱신 이력, 속도, `last_fix`에 반영합니다.
- 거리상 근접 상태와 신뢰도 검증을 통과한 공개 근접 상태를 분리했습니다.
- 비신뢰 근접이 이후 신뢰 상태로 전환되면 `member_adjacency_enter` 이벤트가 정상 발생합니다.
- 현재 좌표가 없거나 필터에 의해 거부되면 근접 엔티티를 `unavailable`로 표시해 오래된 ON/OFF 값을 자동화에 노출하지 않습니다.
- 통합 유형을 계산형 Home Assistant helper로 정리했습니다.
