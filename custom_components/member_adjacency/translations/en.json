{
  "title": "Member Adjacency Distance",
  "config": {
    "step": {
      "user": {
        "title": "Adjacency Sensor",
        "description": "Pick one anchor (A) and multiple targets (B) from *_geocoded_location sensors. Hysteresis: enter<=entry, leave>=exit.",
        "data": {
          "anchor_entity": "Anchor sensor (A)",
          "target_entities": "Target sensors (B)",
          "threshold_preset": "Threshold preset",
          "entry_threshold_m": "Entry threshold (m)",
          "exit_threshold_m": "Exit threshold (m)",
          "debounce_seconds": "Debounce (seconds)",
          "max_accuracy_m": "Max accuracy allowed (m, 0=ignore)",
          "force_meters": "Force meters unit"
        }
      }
    },
    "error": {
      "not_geocoded": "Only *_geocoded_location sensors are allowed.",
      "targets_contains_anchor": "Targets contain the anchor.",
      "targets_empty": "Select at least one target.",
      "exit_lt_entry": "Exit threshold must be >= entry threshold."
    },
    "abort": {
      "already_configured": "This configuration already exists."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Options",
        "description": "Change thresholds/accuracy/debounce/unit.",
        "data": {
          "threshold_preset": "Threshold preset",
          "entry_threshold_m": "Entry threshold (m)",
          "exit_threshold_m": "Exit threshold (m)",
          "debounce_seconds": "Debounce (seconds)",
          "max_accuracy_m": "Max accuracy allowed (m, 0=ignore)",
          "force_meters": "Force meters unit"
        }
      }
    }
  }
}
