{
  "title": "Member Adjacency Distance",
  "config": {
    "step": {
      "user": {
        "title": "Adjacency Sensor",
        "description": "Select two *_geocoded_location sensors and set a threshold (m). If distance is below the threshold, proximity=true.",
        "data": {
          "entity_a": "Entity A",
          "entity_b": "Entity B",
          "proximity_threshold_m": "Proximity threshold (m)"
        }
      }
    },
    "error": {
      "invalid_entity": "Selected entity does not expose coordinates (Location or latitude/longitude).",
      "not_geocoded": "Only *_geocoded_location sensors are allowed."
    },
    "abort": {
      "already_configured": "This pair is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Options",
        "description": "If distance is below the threshold, proximity=true.",
        "data": {
          "proximity_threshold_m": "Proximity threshold (m)"
        }
      }
    }
  }
}
