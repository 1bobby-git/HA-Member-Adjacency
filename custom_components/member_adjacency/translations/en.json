{
  "title": "Member Adjacency Distance",
  "config": {
    "step": {
      "user": {
        "title": "Adjacency Sensor",
        "description": "Configure one A-B pair (zone is not supported). Add again to create another pair like A-C.",
        "data": {
          "entity_a": "Entity A",
          "entity_b": "Entity B",
          "entry_threshold_m": "Entry threshold (m)",
          "exit_threshold_m": "Exit threshold (m)",
          "debounce_seconds": "Debounce (seconds)",
          "max_accuracy_m": "Max accuracy allowed (m, 0=ignore)",
          "force_meters": "Force meters unit"
        }
      }
    },
    "error": {
      "invalid_entity": "Selected entity does not expose coordinates (Location or latitude/longitude).",
      "same_entity": "Entity A and B must be different.",
      "exit_lt_entry": "Exit threshold must be >= entry threshold.",
      "zone_not_supported": "Zone entities are not supported."
    },
    "abort": {
      "already_configured": "This pair is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Options",
        "description": "Change thresholds/accuracy/debounce/unit.",
        "data": {
          "entry_threshold_m": "Entry threshold (m)",
          "exit_threshold_m": "Exit threshold (m)",
          "debounce_seconds": "Debounce (seconds)",
          "max_accuracy_m": "Max accuracy allowed (m, 0=ignore)",
          "force_meters": "Force meters unit"
        }
      }
    }
  }
}
